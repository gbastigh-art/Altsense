=== README ===

See COPYRIGHT.txt for copyright details.

* build with `make libs build` or `make all`
* SUPPORT ONLY FOR WINDOWS LINUX SUBSYSTEM, MAC, AND LINUX
  visual studio coming later, try with mingw if you really want to.
* dependencies:
    cmake
    rsync
    clang (15+)
* if things are broken, ensure that git submodules have been cloned recursively
  (for missing SDL, SDL_Image, cimgui, or imgui)
